[project]
name = "phpstan-laravel-docs"
description = "Builds the phpstan-laravel documentation site"
requires-python = ">=3.10"
version = "0.1.0"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.50",
    "mike @ git+https://github.com/squidfunk/mike.git",
]

[tool.uv]
package = false
